Shinombra v0.1.0
Author: sanlexandro

Use:
  shinombra [FLAGS]

Flags:
  -p, --path <path/to/file> Path to the root manifest (default: ./config.toml)
  -r, --reset               Reset saved PipeWire session token
  -h, --help                Show this text
