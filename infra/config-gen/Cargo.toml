[package]
name = "config-gen"
version = "0.1.0"
edition = "2021"

[dependencies]
config-gen-macros = { path = "../config-gen-macros" }
serde = { version = "1.0", features = ["derive"] }
toml = { version = "0.8", features = ["preserve_order"] }
